# Clara Notes

Clara is a separate project. It shares no vocabulary with the ranking notes on
purpose, so a project filter has something to filter.

## Signals

Signals are a local projection of upstream systems. They are evidence with
upstream provenance, superseded by live upstream state.

## Memory

Distilled facts with weights and reinforcement history. Decay lives inside the
adapter that owns them.
