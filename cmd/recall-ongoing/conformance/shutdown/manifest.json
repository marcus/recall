{
  "case": "shutdown",
  "description": "A clean exit after real work. recall/shutdown takes {} and returns {}; the adapter stops answering, the server finishes in-flight work and returns, and the process closes stdout and exits zero. Nothing is written after the acknowledgement, which is what the replayer's drain checks.",
  "flow": "lockstep",
  "placeholders": {
    "FIXTURE": "absolute path of this case's fixture/ directory",
    "WORKDIR": "absolute path of a fresh, writable, empty directory"
  },
  "volatile": [
    "/result/candidates/*/observed_at",
    "/result/diagnostics/elapsed_ms"
  ],
  "responses": 3
}
